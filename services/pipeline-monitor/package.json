{
  "name": "@lawdigest/pipeline-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Lawdigest data pipeline monitoring web service.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=32240 node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
