# Airflow image dependencies are installed in two phases.
# Dockerfile uses these files directly to avoid applying Airflow constraints
# to app runtime packages such as pydantic-ai-slim.

-r requirements-airflow.txt
-r requirements-runtime.txt
